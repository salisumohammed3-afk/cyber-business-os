[build]
dockerfilePath = "worker/Dockerfile"

[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
# /health is served by orchestrator.mjs on ORCHESTRATOR_PORT (default 3000).
# Railway auto-binds $PORT to the exposed port, so the healthcheck hits it.
healthcheckPath = "/health"
